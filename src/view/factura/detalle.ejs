<!DOCTYPE html>
<html lang="es">

<head>
  <meta charset="UTF-8">
  <title><%= title %></title>

  <%- include("../partials/head") %>
  <link rel="stylesheet" href="/css/factura.css">
</head>

<!-- 🔹 body con clase especial para esta página -->
<body class="bg-overlay factura-page">
  <%- include("../partials/navigation") %>

  <!-- 🔹 wrapper sin vh-100, ya no centramos vertical -->
  <div class="bg-overlay factura-wrapper">
    <div class="container d-flex justify-content-center animate__animated animate__bounceIn">
      <div class="card form-card shadow-sm">

        <div class="card-header bg-dark text-white text-center border-secondary">
          <h3 class="mb-0"><%= title %></h3>
        </div>

        <div class="card-body">

          <div class="mb-3">
            <p class="mb-1"><strong>ID factura:</strong> <%= factura.id_factura %></p>
            <p class="mb-1"><strong>Usuario que facturó:</strong> <%= factura.nombre_usuario || "N/A" %></p>
          </div>

          <div class="mb-3">
            <h5>Datos del registro</h5>

            <p class="mb-1"><strong>Matrícula:</strong> <%= factura.matricula || "" %></p>
            <p class="mb-1"><strong>Cliente:</strong> <%= factura.nombre_cliente || "" %></p>
            <p class="mb-1"><strong>Fecha:</strong> <%= factura.fecha || "" %></p>
            <p class="mb-1"><strong>Hora entrada:</strong> <%= factura.hora_entrada %></p>
            <p class="mb-1"><strong>Hora salida:</strong> <%= factura.hora_salida %></p>
          </div>

          <div class="mb-3">
            <h5>Multa</h5>
            <% if (factura.multa_descripcion) { %>
              <p class="mb-1"><strong>Descripción:</strong> <%= factura.multa_descripcion %></p>
              <p class="mb-1"><strong>Monto multa:</strong> $<%= factura.multa_monto %></p>
            <% } else { %>
              <p class="mb-1">Sin multa aplicada.</p>
            <% } %>
          </div>

          <div class="mb-3">
            <h5 class="section-title">Total</h5>
            <p class="fs-4">
              <strong>Total factura:</strong>
              $<%= parseFloat(factura.total_factura).toFixed(2) %>
            </p>
          </div>

          <div class="d-flex justify-content-between mt-4">
            <button type="button" class="btn btn-print" onclick="window.print()">
              🖨 Imprimir
            </button>

            <a href="/factura" class="btn btn-secondary">
              Volver a listado
            </a>
          </div>

        </div>

      </div>
    </div>
  </div>

</body>
</html>

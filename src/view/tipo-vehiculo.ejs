<!DOCTYPE html>
<html lang="es">

<head>
  <meta charset="UTF-8">
  <title>
    <%= title %>
  </title>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

  <%- include("partials/head") %>
    <link rel="stylesheet" href="/css/tipoVehiculo.css">
</head>

<body class="bg-overlay">

  <%- include("partials/navigation") %>

    <div class="bg-overlay">
    <div class="container vh-100 d-flex justify-content-center align-items-center animate__animated animate__bounceIn">
      <div class="card animated flipInY form-card shadow-sm">

        <div class="card-header bg-dark text-white text-center border-secondary">
          <h3 class="mb-0">
            <%= title %>
          </h3>
        </div>

        <div class="card-body">

          <% if (error) { %>
            <div class="alert alert-danger text-center">
              <%= error %>
            </div>
            <% } %>

              <form
                action="<%= tipo.id_tipo_vehiculo ? '/tipo-vehiculo/editar/' + tipo.id_tipo_vehiculo : '/tipo-vehiculo/agregar' %>"
                method="POST" autocomplete="off">

                <input type="hidden" name="redirectTo" value="<%= redirectTo || '' %>">

                <div class="mb-3">
                  <label for="nombre" class="form-label">Nombre del tipo:</label>
                  <input id="nombre" name="nombre" type="text" class="form-control bg-dark text-white border-secondary"
                    placeholder="Automóvil, Motocicleta, Camioneta..." value="<%= tipo.nombre || '' %>" required>
                </div>

                <div class="mb-3">
                  <label for="tarifa_hora" class="form-label">Tarifa por hora ($):</label>
                  <input id="tarifa_hora" name="tarifa_hora" type="number" step="0.01" min="0"
                    class="form-control bg-dark text-white border-secondary" placeholder="1.50"
                    value="<%= tipo.tarifa_hora || '' %>" required>
                </div>

                <div class="d-grid gap-2 mt-3">
                  <button type="submit" class="btn btn-primary">Guardar</button>
                  <a href="/tipos-vehiculo/lista" class="btn btn-outline-secondary">Ir a lista</a>
                </div>

              </form>

        </div>
      </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
      crossorigin="anonymous"></script>

</body>
</html>
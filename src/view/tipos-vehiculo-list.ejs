<!DOCTYPE html>
<html lang="es">

<head>
  <meta charset="UTF-8">
  <title>Tipos de Vehículo</title>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

  <%- include("partials/head") %>
    <link rel="stylesheet" href="/css/tipoVehiculo.css">
</head>

<body class="bg-overlay">
  <%- include("partials/navigation") %>
    <div class="bg-overlay">
    <div class="container-box animate__animated animate__bounceIn">
       <% if (error) { %>
      <div class="alert alert-danger text-center">
        <%= error %>
      </div>
    <% } %>

    <% if (msg) { %>
      <div class="alert alert-success text-center">
        <%= msg %>
      </div>
    <% } %>
      <h3 class="text-center mb-4">Tipos de Vehículo</h3>

      <table class="table table-dark table-hover text-center align-middle">
        <thead>
          <tr>
            <th>Nombre</th>
            <th>Tarifa por hora ($)</th>
            <th>Acciones</th>
          </tr>
        </thead>
        <tbody>
          <% if (tipos && tipos.length> 0) { %>
            <% tipos.forEach(t=> { %>
              <tr>
                <td><%= t.nombre %></td>
                <td><%= t.tarifa_hora %></td>

                <td>
                  <a href="/tipo-vehiculo/editar/<%= t.id_tipo_vehiculo %>" class="btn btn-sm btn-success me-1">
                    ✏ Editar
                  </a>
                  <a href="/tipo-vehiculo/eliminar/<%= t.id_tipo_vehiculo %>" class="btn btn-sm btn-danger"
                    onclick="return confirm('¿Seguro que deseas eliminar este tipo de vehículo?');">
                    🗑 Eliminar
                  </a>
                </td>
              </tr>
              <% }) %>
                <% } else { %>
                  <tr>
                    <td colspan="4">No hay tipos de vehículo registrados.</td>
                  </tr>
                  <% } %>
        </tbody>
      </table>

      <div class="text-center mt-3">
        <a href="/tipos-vehiculo" class="btn btn-primary">Registrar nuevo tipo</a>
      </div>
      <div class="text-center mt-3">
        <a href="/vehiculos" class="btn btn-warning">⬅ Volver a Registro de Vehículos</a>
      </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
      crossorigin="anonymous"></script>

</body>
</html>
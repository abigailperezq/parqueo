<!DOCTYPE html>
<html lang="es">

<head>
  <meta charset="UTF-8">
  <title><%= title %></title>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <%- include("partials/head") %>
  <link rel="stylesheet" href="/css/multa.css">
</head>

<body class="bg-overlay">

  <%- include("partials/navigation") %>

  <div class="container vh-100 d-flex justify-content-center align-items-center animate__animated animate__bounceIn">

    <div class="card form-card shadow-sm">

      <div class="card-header text-center">
        <h3 class="mb-0"><%= title %></h3>
      </div>

      <div class="card-body">

        <% if (error) { %>
          <div class="alert alert-danger text-center"><%= error %></div>
        <% } %>

        <form action="<%= multa.id_multa ? '/multa/editar/' + multa.id_multa : '/multa/agregar' %>" method="POST">

          <div class="mb-3">
            <label class="form-label">Descripción:</label>
            <input type="text" name="descripcion"
                   class="form-control bg-dark text-white border-secondary"
                   value="<%= multa.descripcion || '' %>" required>
          </div>

          <div class="mb-3">
            <label class="form-label">Monto ($):</label>
            <input type="number" step="0.01" min="0.01"
                   name="monto"
                   class="form-control bg-dark text-white border-secondary"
                   value="<%= multa.monto || '' %>" required>
          </div>

          <div class="d-grid gap-2 mt-3">
            <button class="btn btn-primary">Guardar</button>
            <a href="/multa-list" class="btn btn-outline-secondary">Ir a lista</a>
          </div>

        </form>

      </div>
    </div>

  </div>

</body>
</html>